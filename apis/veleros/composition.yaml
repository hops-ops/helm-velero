apiVersion: apiextensions.crossplane.io/v1
kind: Composition
metadata:
  name: veleros.helm.hops.ops.com.ai
spec:
  compositeTypeRef:
    apiVersion: helm.hops.ops.com.ai/v1alpha1
    kind: Velero
  mode: Pipeline
  pipeline:
  - functionRef:
      name: hops-ops-helm-velerorender
    step: render
  - functionRef:
      name: crossplane-contrib-function-auto-ready
    step: crossplane-contrib-function-auto-ready
